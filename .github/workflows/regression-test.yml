# name: Regression Tests

# on:
#   push:
#     branches:
#       - develop
#       - main
#   pull_request:
#     branches:
#       - develop
#       - main
#   schedule:
#     - cron: '0 20 * * *'
#   workflow_dispatch:

# jobs:
#   regression-tests:
#     timeout-minutes: 60
#     runs-on: ubuntu-latest

#     env:
#       SEP_USERNAME: ${{ secrets.SEP_USERNAME }}
#       SEP_PASSWORD: ${{ secrets.SEP_PASSWORD }}
#       SEP_QA_URL: ${{ secrets.SEP_QA_URL }}
#       CARD_NUMBER: ${{ secrets.CARD_NUMBER }}
#       CARD_EXPIRATION_DATE: ${{ secrets.CARD_EXPIRATION_DATE }}
#       CARD_SECURITY_CODE: ${{ secrets.CARD_SECURITY_CODE }}
#       ZIP_CODE: ${{ secrets.ZIP_CODE }}

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: 20
#           cache: npm

#       - name: Install dependencies
#         run: npm ci

#       - name: Install Playwright browsers
#         run: npx playwright install --with-deps

#       - name: Run regression tests
#         run: npm run test

#       - name: Upload Cucumber report
#         if: always()
#         uses: actions/upload-artifact@v4
#         with:
#           name: cucumber-report
#           path: reports/
#           if-no-files-found: ignore
#           retention-days: 30